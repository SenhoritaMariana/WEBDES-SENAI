let n1 = 82
let n2 = -4

console.log(Math.abs(n2)) // Transforma em número absoluto

console.log(Math.ceil(3.14)) // Arredonda para cima

console.log(Math.floor(3.8)) // Arredonda para baixo

console.log(Math.round(3.8)) // Arredonda para o número mais próximo

let nota1 = 10
let nota2 = 7
let nota3 = 6

console.log(Math.max(nota1, nota2, nota3)) // Retorna o maior valor
console.log(Math.min(nota1, nota2, nota3)) // Retorna o menor valor

console.log(Math.pow(2, 3)); // O primeiro valor é a base e o segundo é o expoente

console.log(Math.sqrt(9)); // Retorna a Raiz Quadrada

console.log(Math.log(2)); // Retorna o logaritmo natural

console.log(Math.sin(2)); // Calcula o Seno
console.log(Math.con(2)); // Calcula o Coseno
console.log(Math.tan(2)); // Calcula a Tangente

let dinheiro = 7.578

console.log(dinheiro.toFixed(2));
console.log(dinheiro.toFixed(5));